# 部署后首次设置

## 🎯 部署完成后必须做的事

### 1. 创建管理员账户

SSH连接到服务器后：
```bash
cd /opt/reimbursement_system
source venv/bin/activate
python create_admin.py
```

按提示输入管理员信息。

### 2. 测试访问

在浏览器访问：`http://您的服务器IP`

应该能看到登录页面。

### 3. 登录并配置

1. 使用刚创建的管理员账户登录
2. 进入"用户管理"页面
3. 为其他用户分配角色

### 4. 检查服务状态

```bash
# 检查Gunicorn
sudo systemctl status reimbursement

# 检查Nginx
sudo systemctl status nginx

# 如果服务未运行，启动它们
sudo systemctl start reimbursement
sudo systemctl start nginx
```

### 5. 配置云服务器安全组

在云服务器控制台：
- 进入"安全组"或"防火墙"设置
- 添加入站规则：
  - 端口：80（HTTP）
  - 端口：443（HTTPS，如果配置了SSL）
  - 端口：22（SSH，用于远程管理）

### 6. 测试功能

- [ ] 注册新用户
- [ ] 创建报销单
- [ ] 上传发票
- [ ] 审核报销单
- [ ] 导出功能

## 🔍 故障排查

### 无法访问网站
1. 检查服务是否运行：`sudo systemctl status reimbursement`
2. 检查防火墙：`sudo ufw status`
3. 检查云服务器安全组设置
4. 查看日志：`sudo journalctl -u reimbursement -n 50`

### 502错误
1. 检查Gunicorn服务：`sudo systemctl status reimbursement`
2. 检查socket文件权限
3. 查看错误日志

### 静态文件404
运行：`python manage.py collectstatic --noinput`

## 📞 获取帮助

查看详细日志：
```bash
# Gunicorn日志
sudo journalctl -u reimbursement -f

# Nginx错误日志
sudo tail -f /var/log/nginx/error.log

# Nginx访问日志
sudo tail -f /var/log/nginx/access.log
```




