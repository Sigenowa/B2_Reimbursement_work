# 快速部署检查清单

## ✅ 部署前检查

- [ ] 云服务器已购买并启动
- [ ] 已获取服务器IP地址和root密码
- [ ] 可以通过SSH连接到服务器
- [ ] 项目文件已准备好

## 📋 部署步骤（简化版）

### 1. 连接服务器
```bash
ssh root@您的服务器IP
```

### 2. 上传项目文件
使用WinSCP或FileZilla上传整个项目文件夹到 `/opt/reimbursement_system/`

### 3. 运行部署脚本（推荐）
```bash
cd /opt/reimbursement_system
chmod +x deploy.sh
./deploy.sh
```

### 4. 或手动部署（见详细指南）
按照 `云服务器部署指南.md` 中的步骤操作

### 5. 创建管理员
```bash
cd /opt/reimbursement_system
source venv/bin/activate
python create_admin.py
```

### 6. 访问网站
打开浏览器访问：`http://您的服务器IP`

## 🔧 常用命令

```bash
# 查看服务状态
sudo systemctl status reimbursement

# 重启服务
sudo systemctl restart reimbursement

# 查看日志
sudo journalctl -u reimbursement -f

# 停止服务
sudo systemctl stop reimbursement

# 启动服务
sudo systemctl start reimbursement
```

## 🌐 配置域名（可选）

如果有域名：
1. 在域名DNS中添加A记录，指向服务器IP
2. 修改Nginx配置中的server_name
3. 使用Certbot配置HTTPS

## ⚠️ 重要提醒

1. **首次部署后必须创建管理员账户**
2. **确保防火墙开放80和443端口**
3. **生产环境务必设置强密码**
4. **定期备份数据库和文件**




