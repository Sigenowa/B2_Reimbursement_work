# 本地部署指南

## 📋 概述

第四周实现本地部署功能，让您可以在自己的电脑上完整运行报销神表系统，支持局域网访问。

## 🔧 系统要求

### Windows系统
- Windows 10/11
- Python 3.9 或更高版本
- MySQL 8.0 或更高版本
- 至少4GB可用内存

### Linux/Mac系统
- Python 3.9+
- MySQL 8.0+
- Bash shell

## 📦 安装步骤

### 第一步：安装Python

**Windows:**
1. 下载Python：https://www.python.org/downloads/
2. 安装时勾选 "Add Python to PATH"
3. 验证安装：`python --version`

**Linux:**
```bash
sudo apt update
sudo apt install python3 python3-pip -y
```

**macOS:**
```bash
brew install python3
```

### 第二步：安装MySQL

**Windows:**
1. 下载MySQL：https://dev.mysql.com/downloads/mysql/
2. 选择 "MySQL Installer for Windows"
3. 安装时选择 "Developer Default"
4. 记住设置的root密码

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install mysql-server -y
sudo systemctl start mysql
sudo systemctl enable mysql
```

**macOS:**
```bash
brew install mysql
brew services start mysql
```

### 第三步：克隆或下载项目

确保项目文件在本地。

### 第四步：运行部署脚本

**Windows:**
```bash
# 在项目根目录运行
local_deploy.bat
```

**Linux/Mac:**
```bash
# 在项目根目录运行
chmod +x local_deploy.sh
./local_deploy.sh
```

## 🚀 启动服务器

### Windows
双击 `start_server.bat` 或运行：
```bash
python manage.py runserver 0.0.0.0:8000
```

### Linux/Mac
运行 `./start_server.sh` 或：
```bash
python3 manage.py runserver 0.0.0.0:8000
```

## 🌐 访问系统

### 本地访问
打开浏览器访问：http://127.0.0.1:8000

### 局域网访问
1. 获取本机IP地址
2. 其他设备访问：http://[您的IP]:8000

**Windows获取IP：**
```bash
ipconfig | findstr IPv4
```

**Linux获取IP：**
```bash
hostname -I
```

## ⚙️ 环境变量配置

创建 `.env` 文件（可选）：
```env
DB_NAME=reimbursement_db
DB_USER=reimbursement_user
DB_PASSWORD=password123
DB_HOST=localhost
DB_PORT=3306
```

## 🔍 故障排除

### MySQL连接失败
1. 检查MySQL服务是否运行
2. 验证用户名和密码
3. 检查防火墙设置

### 端口被占用
```bash
# Windows
netstat -ano | findstr :8000

# Linux
netstat -tlnp | grep :8000
```

### 依赖安装失败
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 数据库迁移失败
```bash
python manage.py migrate --verbosity=2
```

## 📱 移动端访问

确保手机和电脑在同一WiFi网络下：
- iPhone Safari：http://[电脑IP]:8000
- Android Chrome：http://[电脑IP]:8000

## 🔥 防火墙配置

### Windows
1. 打开"Windows安全中心"
2. 防火墙和网络保护 → 高级设置
3. 入站规则 → 新建规则
4. 端口 → TCP 8000 → 允许连接

### Linux
```bash
sudo ufw allow 8000
```

## ✅ 部署验证

- [ ] Python环境正常
- [ ] MySQL服务运行
- [ ] 数据库连接成功
- [ ] Django迁移完成
- [ ] 服务器启动成功
- [ ] 本地访问正常
- [ ] 局域网访问正常

## 📊 性能优化

### 开发环境配置
```python
# settings.py
DEBUG = True
ALLOWED_HOSTS = ['*']
```

### 生产环境建议
```python
# settings.py
DEBUG = False
ALLOWED_HOSTS = ['localhost', '127.0.0.1', '您的IP地址']
```

## 🛠️ 常用命令

### 数据库管理
```bash
# 重新迁移
python manage.py migrate --run-syncdb

# 创建超级用户
python manage.py createsuperuser

# 备份数据库
python manage.py dumpdata > backup.json

# 恢复数据库
python manage.py loaddata backup.json
```

### 服务器管理
```bash
# 指定端口启动
python manage.py runserver 0.0.0.0:8080

# 指定设置文件
python manage.py runserver --settings=my_settings.py
```

<<<<<<< HEAD

=======
## 📞 获取帮助

如果遇到问题，请检查：
1. 项目目录结构是否完整
2. 所有依赖是否正确安装
3. 数据库配置是否正确
4. 防火墙是否阻止了访问
>>>>>>> 83f6b4b25d571371466929dc1113e7582b7000cb
